# DTC Enterprise Frontend
React + Vite frontend for DTC using FastAPI + Supabase + Render backend.

## Setup
1. Edit `src/config/apiConfig.js` and set your Render API URL.
2. Run `npm install`
3. Run `npm run dev`
